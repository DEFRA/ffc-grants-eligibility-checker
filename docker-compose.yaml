version: '3.9'

services:
  ffc-grants-eligibility-checker:
    build:
      context: .
      target: ${NODE_ENV}
    image: ffc-grants-eligibility-checker  
    platform: ${PLATFORM}
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: ${NODE_ENV}

