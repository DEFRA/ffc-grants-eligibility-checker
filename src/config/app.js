import { name, description, version } from "package.json";
export const app = {
    // Application data
    name,
    description,
    version,

    // Local Development
    host: "localhost",
    port: 3000,
}
